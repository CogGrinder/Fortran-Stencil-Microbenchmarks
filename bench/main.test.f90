#include "src/benchmark_compilation_fixed_parameters.h"

program main
    ! integer :: nx, ny
    ! nx = (NX(BENCHMARK_SIZE_MODE))
    ! ny = (NY(BENCHMARK_SIZE_MODE,(NX(BENCHMARK_SIZE_MODE))))
    write(*,*) "nx", nx
    write(*,*) "ny", ny
end program main